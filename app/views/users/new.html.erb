<% if params[:kind] == "Brand" %>
  <%= render 'form_brand', user: @user %> <!-- #renders if user signup is for Brand -->
<% else %>
  <%= render 'form_amb', user: @user %> <!-- #renders if user signup is for Brand -->
<% end %>



